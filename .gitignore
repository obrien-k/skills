# local Claude Code state
.claude/settings.local.json
.claude/*.lock

# playwright MCP scratch (console logs, screenshots)
.playwright-mcp/

# stray screenshots
resume-*.png

# local TDD scratch — verification only, never shipped
*.test.js

# dependencies
node_modules
